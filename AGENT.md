# AGENT.md

## 项目概览

这个仓库是一个基于 Jupyter 的学习工程，主要用于练习机器学习和量化交易。

- `tutorial/`：机器学习相关教程、实验、Notebook 和小型数据集。
- `trade_tutorial/`：机器学习与量化交易结合的脚本、Notebook 和实验。
- `requirements.txt`：学习 Notebook 使用的 Python 依赖版本。

这个项目以学习和练习为主。编写代码时，优先保证示例清晰、容易理解、方便复现，不要过度追求生产级抽象。

## 工作方式

- 示例应尽量简单、可复现。
- 保持 Notebook 的可读性：先说明学习目标，再展示代码，最后简要解释结果。
- 除非用户明确要求，不要引入大的框架调整。
- 优先做小步、增量式修改，并保持现有目录结构。
- 不要主动移动或重命名 Notebook、数据集、教程目录。
- 编辑 Notebook 时，尽量避免提交过大的输出结果或生成文件。

## 目录约定

### `tutorial/`

这个目录用于通用机器学习学习内容，例如：

- 模型拟合与评估
- 正则化
- 线性回归与优化
- 特征工程与特征选择
- PCA 与降维
- 基于 `tutorial/data/` 中小型 CSV 数据集的实验

新增机器学习内容时，优先放在按主题划分的子目录中，例如：

- `tutorial/chXX_topic_name/`
- `tutorial/feature/`
- `tutorial/model_selection/`
- `tutorial/data/`

### `trade_tutorial/`

这个目录用于机器学习与量化交易结合的内容，例如：

- 因子研究
- 基于行情数据的特征构造
- 收益率预测实验
- 交易信号生成
- 回测示例
- 组合构建或风险控制练习

编写量化相关示例时，要明确金融假设。例如：预测窗口、调仓频率、交易成本、基准选择，以及标签是否使用了未来收益。

## Python 与 Notebook 约定

- 按照 `requirements.txt` 中的依赖版本使用 `pandas`、`numpy`、`matplotlib`、`seaborn` 和 `scikit-learn`。
- 涉及随机性的示例，尽量设置随机种子，保证结果可复现。
- 数据路径使用相对于仓库根目录或 Notebook 所在目录的相对路径。
- 避免写死本机绝对路径。
- 图表应有清晰的标题、坐标轴标签和图例。
- 教学型 Notebook 中，优先使用命名清楚的中间变量，避免过度压缩成复杂的一行代码。

## 量化交易约定

创建或检查量化相关代码时：

- 注意避免未来函数、前视偏差和数据泄漏。
- 尽量拆分特征生成、标签生成、模型训练、预测和回测逻辑。
- 时间序列任务中的训练集和测试集划分应符合时间顺序；不要随意随机打乱，除非该示例就是为了说明这样做的问题。
- 应考虑交易成本；如果暂时忽略交易成本，需要明确说明。
- 所有结果都应视为学习示例，不构成投资建议。

## 数据处理

- 机器学习教程使用的小型样例数据，放在 `tutorial/data/`。
- 量化交易示例如果需要数据文件，优先放在 `trade_tutorial/` 下的专门数据目录中。
- 不要提交大型原始数据集、API Key、账号凭证或私人数据。
- 如果示例依赖外部数据，需要说明数据来源和预期字段结构。

## 验证方式

完成修改前：

- 如果是 Python 脚本，尽量运行修改过的脚本。
- 如果是 Notebook，至少检查导入语句和关键单元是否与当前依赖兼容。
- 如果新增依赖，需要更新 `requirements.txt`，并说明为什么需要该依赖。
- 不要把 `__pycache__/`、`.ipynb_checkpoints/` 等缓存文件加入版本控制。

## 沟通说明

这个仓库主要用于练习和学习。解释修改时，应尽量带上学习视角：这个文件演示了什么概念、为什么这样组织、接下来可以继续尝试什么。
